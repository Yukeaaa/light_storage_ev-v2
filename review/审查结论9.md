可以正式放行了。

审查结论9：S0 正式 PASS

我已复核 cb78140c46265cd49ec181620b16c7eeebcdcc9c，当前 main 与该提交一致。提交说明与实际修改吻合，审查结论8要求的 S0.1 修复均已落地。

几个关键点我逐项确认过：

split schema 已统一为 train / validation / test / external / stress 五值，并加入 connection_time_source；此前最直接的 schema 冲突已经消失。
assign_split() 已使用 [connection_time, session_id] + mergesort，并新增相同连接时间、3个不同随机打乱顺序的确定性测试；分钟级输入拒绝仍保留。
canonical connection time 已冻结为 matched 优先 API connectionTime，fallback 单独标记 first_observation_fallback，不会再把“首条观测时间”和“真实连接时间”无痕混在一起。
C-004 已明确要求 E1-Full + E2/K2 同时满足；A/B/C/D证据边界、source_ref 和来源回查机制已经建立。
README 中“理论上可被其他车辆重新利用”的越界措辞已经删除，改成“预算修正候选窗口，是否可被其他车辆吸收尚未证明”。这一版可以继续作为项目问题定义。
本机路径退出版本控制、增加 paths.example.yaml 的处理也是正确的；io/paths.py 仍保持延迟读取，不会仅因 import 就要求本地路径文件存在。
外部证据也再次核验过

这一点尤其重要，因为我们之前明确要求“背景不能靠想当然”。

官方 ChargeX 报告首页确实是 ANL-25/62，October 2025；报告正文明确解释 SAE J1772 pilot 控制的是 maximum allowable ampacity，并明确说它应被视为 limit rather than setpoint。报告也对25个品牌车辆测出了 accuracy、latency、precision、resolution 等响应差异。

Caltech 的 ACN 2021 论文 DOI 10.1109/TSG.2021.3074437 也核对正确，其摘要明确列出真实充电系统中的 non-ideal battery charging behavior 和 quantized control signals。 2018 SmartGridComm 的 DOI 10.1109/SmartGridComm.2018.8587550 和 ACN-Data 2019 的 DOI 10.1145/3307772.3328313 也与仓库登记一致。

所以目前问题背景—公开依据—本项目K1数据证据三层已经能够互相对上。

有两个实施细节不再阻塞 S0，但必须写进新 Issue 的验收条件

这两个不需要再搞 S0.2，也不应继续拖延全量阶段。

第一，matched_fallback 里目前还有“API connectionTime 缺失/异常”的措辞。 E0F-01 实现时要规定得更保守：

缺失或无法解析才允许自动 fallback；如果 API connectionTime 可解析但与时序数据明显矛盾，不得自动替换，应登记异常并进入数据质量审计/停止线。

这样不会让工程师在看到数据后主观决定什么叫“异常”。

第二，JPL 的角色纪律必须继续锁死。K1 预注册明确规定：Caltech 是主集；JPL 2020-06/07 是只评估、不调参的外部边界；JPL current-only 是保护回退证据池。 E0F-02 即使给 JPL 会话生成时间 split，也必须同时保留其 boundary/current_only_fallback 数据角色，后续任何 train/validation 标签都不得被误解为可以参与主模型调参。

这两个属于实施防误用要求，不是新的研究规则。

现在可以正式切换阶段

DD-001 当前仍是 open，且没有关闭评论。

所以现在应该正式执行：

在 DD-001 留最终结论：K1 条件 Go、S0 正式 PASS、风险控制顺序已执行，并关闭为 completed。
创建 E0F-01：全量输入 manifest + 数据质量审计。
创建 E0F-02：canonical connection time + 硬时间 split + field mode/role registry。
创建 E0F-03：session_response_1min 标准数据集。
创建 E0F-04：pool_state_1min/5min。
创建 E0F-05：D0 数据工程验收。
创建 E0F-06：R1 K1 硬切分复现。

可以现在准备并创建这些 Issue。

不过创建 Issue 后，也不要六个一起开工。执行顺序仍然是：

E0F-01 → E0F-02 → E0F-03 → E0F-04 → D0(E0F-05) → R1(E0F-06)

其中 E0F-01/02 出现 stop-line 时立即停，不因为已经开始全量工程就“顺手把后面也做了”。

关于测试状态

仓库提交记录写的是 60 tests 全绿、S0 文件 ruff 通过，我审查到的新增测试也与这个数量增长逻辑一致。 但当前 GitHub connector 没有返回 cb78140 的独立 Actions 状态，因此我不把“远端 CI green”作为已独立验证事实。现有 .github/workflows/ci.yml 仍是干净 checkout → Python 3.12 → dev install → pytest。

这不阻止 S0 PASS；进入 E0F-01 后，建议把“fresh clone 无 paths.yaml 时 pytest 可通过”作为第一项工程卫生检查。

正式门结论：

S0：PASS。
批准进入 E0-Full 数据工程阶段。
批准创建并启动 E0F-01～06 管理任务。
实际工程先启动 E0F-01；E2、E4、多车主动增配、闭环收益仍然禁止启动。

这一阶段终于可以开始动全量数据了，但仍然保持我们的原则：先把数据底座和K1复现做死，再决定是否值得投入模型。