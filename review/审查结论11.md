我已经完成正式复核。GitHub 现在能看到这两个提交，而且 main 相对 ee637a5 正好多出 9143c68 → a23f6fd 两个提交，所以可以按真实代码和冻结产物审查，而不是只依据你的摘要。

审查结论11：E0F-01.1 主体 PASS，但 raw-duplicate gate 暂不解除

这轮修复质量明显比上一轮高。baseline 追溯、duplicate 机器化证据、站点映射三个上一轮核心问题已经解决。 但我发现一个新的 P0：当前 collapse 影响分析只比较了原始字段，没有按照项目冻结的功率派生规则继续传播到 actual_power_kw。因此现在还不能用“power 零影响”来批准 gate amendment。

已正式通过的部分

1. 两段式追溯链现在是正确的。 当前 frozen baseline 明确记录 code_sha=9143c684...，parent_baseline.commit=41dd1dd...，同时记录 code_dirty_files=[]、code_clean=true。这已经把“生成证据的代码版本”和“历史 K0 baseline 锚点”分开了。

代码实现也确实不是简单改 JSON：parent_baseline.commit 从 design_baseline.json 读取，正式 frozen run 遇到已跟踪的非证据代码修改会拒绝生成 baseline。

2. 上一轮“亚秒采样”过度解释已经纠正。 现在代码明确把它命名为 same_timestamp_distinct_rows，并注明“具体采样机制未被当前数据证明”；同时间戳不同观测值保留进入确定性分钟聚合。这个表述是合适的。

3. 663 行 exact duplicates 已真正机器化。 现在不是人工观察，而是代码按 exact line count、zero-idle/nonzero、site、garage、station、month、role 输出。证据确认：

exact duplicate 663；
JPL 663，Caltech/Office 0；
661 zero-idle；
2 nonzero；
K1-M frozen exact duplicate = 0；
JPL boundary 2020 exact duplicate = 0；
JPL current-only window = 109，均 zero-idle。

而且你主动修正“663 全部是零值”的旧结论，这是正确的证据治理方式。

4. office_01 → office001 映射已冻结。 配置明确保留 raw/canonical 两层，并禁止下游临时 .replace()；这解决了 E0F-02 最危险的 Office 被误纳入主 split 的风险。

5. STOP 没被偷偷删除。 当前报告仍明确写 dup_ts_within_file = FAIL、总体 STOP，#12 的评论也完整保留了 4 PASS / 1 FAIL 和 #13 BLOCKED 的治理状态。

这些部分，我认为可以正式 PASS。

现在真正的 P0：JPL current-only 的“power 零影响”并不成立

当前影响量代码比较的是原始 CSV 中的几个字段：

current / power / pilot 做分钟均值，energy 做分钟末值。

结果确实显示 raw power 列差值为 0；但这是因为大量 JPL 文件本来就是 current-only，原始文件里没有 power。

而 E0-Full 已冻结的功率规则明确是：

measured power → voltage×current → rated voltage×current estimated，JPL rated voltage = 192.7 V。

所以对 JPL current-only：

P
actual
	​

=I×192.7/1000

你当前已经测出 collapse 前后：

JPL current-only：71 个分钟 current 改变；
最大 current 差 2.4667 A。

这意味着最大派生功率差约：

2.4667×192.7/1000≈0.475 kW

而不是 0。

0.475 kW 已经不是可以直接无视的量级，因为我们的 K1/E3 里本身存在 0.5 kW 左右的工作/事件阈值。

所以报告里的：

power / pilot / energy 均 0 影响

只能准确改成：

raw power column = 0 impact；derived actual_power 尚未评估。

当前这个问题是我不批准立即解除 STOP 的主要原因。

为什么这不是吹毛求疵

JPL current-only 并不是无关数据。

冻结配置把它定义为保护型回退证据池；E3 也用它作为第二证据池。当前配置仍明确保留 current_only_fallback 角色。

所以 exact duplicate 虽然：

不污染 Caltech K1-M；
不污染 JPL K1-X boundary；

但确实可能影响 JPL current-only 的派生功率轨迹。

好消息是，这个影响大概率很小：只有 71 分钟发生变化，而且所有 109 个 exact duplicates 都是 zero-idle。但我们不能凭“大概率”批准 gate，尤其前一个项目就是因为过早乐观判断投入过大。

我建议只做一个非常小的 E0F-01.2，不要再做大工程

不需要重新扫描 4.48 亿行，也不要开始 E0F-02。

只补下面这组 gate-resolution sensitivity。

1. 把 collapse impact 扩展到 actual_power_kw

必须复用正式冻结的功率优先级，而不是再实现一套简化版本：

measured power
→ voltage × current
→ rated_voltage × current

特别输出：

actual_power_kw:
  affected_minutes
  max_abs_diff_kw
  p95_abs_diff_kw
  mean_abs_diff_kw
  total_abs_energy_diff_kwh

JPL current-only 使用 192.7 V。

2. 对 JPL current-only 做一次 keep-vs-collapse 的冻结指标敏感性

不需要全 K1 重跑。

只对受影响 current-only 文件/会话，分别按：

A. raw exact duplicates 保留
B. 派生层 exact duplicates collapse

经过同一套正式 power derivation + 1min + 5min pipeline。

至少比较：

current_only_low_power_state_frequency
E3 A2_prev_actual candidate rate
daily candidate energy share
发生 candidate/non-candidate 翻转的 5min cycle 数
active/inactive 状态翻转数
是否导致任一冻结 gate 结论改变

尤其是 JPL current-only 的冻结日候选能量份额此前约 3.9%，停止线只有 0.5%；如果 collapse 后仍有充分裕量，就有很强的正式放行依据。

建议再修一个 P1：role 目前仍是“窗口代理”，不是严格 K1 eligibility

现在 file_role() 的判定基本是：

site + garage + month

比如 Arroyo + frozen months 就直接标记 jpl_current_only。

但真正 K1 current-only 的定义还有：

has_current
no_pilot

boundary 也要求 matched/pilot/power 条件。

所以严格说，现在这些名字最好叫：

jpl_current_only_window
jpl_boundary_window
caltech_main_window

或者加入相应 eligibility 信息后再称 frozen role。

从目前数据分布看，这不太可能改变 exact-duplicate 主结论，但对证据台账语言最好别把“月份窗口”直接等同于“最终样本角色”。

baseline 还有一个低优先级增强

目前 _git_dirty_code() 对所有 ?? untracked 文件直接忽略。

这对当前两段式运行未必造成实际问题，但长期来看，untracked 的 .py/.yaml/.json 也可能参与运行。

建议 D0 前改成：

证据输出白名单允许 dirty/untracked；
src/、configs/、experiments/、tests/ 下任何 untracked 文件都让 code_clean=false。

这不是本轮 blocker。

raw-duplicate gate 最终怎么解，我现在可以把规则先定出来

只要上面的 derived-power/K1 sensitivity 通过，我支持正式做 amendment。

而且不要把历史 STOP 改成 PASS。历史上确实触发过 STOP，应保留。

建议结构改成：

initial_stop:
  dup_ts_within_file:
    triggered: true
    files: 10684

resolution:
  status: resolved
  decision: raw_timestamp_multiplicity_allowed_with_controls
  evidence_sha: ...

正式数据契约可以变成：

原始时序允许存在同一记录时间戳的多条观测，必须完整保留并登记；同时间戳不同值的观测不得任意删除，进入冻结的确定性分钟聚合。逐字节完全相同的重复原始行不得修改源文件，在派生层按照冻结的 exact-duplicate collapse 规则处理，并保留 raw_duplicate_count/质量标记。D0 uniqueness 约束施加于 canonical session_id × timestamp_utc(1min) 数据集。只有在 exact-duplicate collapse 对冻结 K1 gate 结论不产生实质变化后，原始重复 STOP 才可标记为 resolved。

这才是“解释清楚数据问题”，而不是“为了过关把阈值从 0 改大”。

当前正式门结论
审查项	结论
E0F-01.1 baseline traceability	PASS
两段式冻结流程	PASS
duplicate 机器化分类	PASS
旧错误表述修正	PASS
site canonical mapping	PASS
原始输入只读	PASS
Caltech K1-M exact duplicate 污染	无
JPL K1-X exact duplicate 污染	无
JPL current-only exact duplicate	存在，109 行
raw-power impact	已测
derived actual-power impact	未测，P0
K1 current-only gate sensitivity	未测，P0
raw duplicate gate	继续 STOP
E0F-02	继续 BLOCKED

因此现在不需要大改。

下一步只做 E0F-01.2：补 actual_power_kw 派生影响 + JPL current-only keep/collapse K1 敏感性。 如果这两个结果证明 gate 结论不变，我预计下一轮就可以正式签署 raw-duplicate gate amendment，关闭 #12，并解锁 #13。

这次已经非常接近真正放行 E0F-02 了，但这个 0.475 kW 的派生功率问题值得最后堵死。