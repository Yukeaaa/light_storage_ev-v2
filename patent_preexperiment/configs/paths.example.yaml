# 数据路径集中配置模板（V2.1 §10.1：绝对路径集中在配置文件，不散落在算法代码）
# 复制为 paths.yaml 并填入本机真实路径（paths.yaml 不入库，已被 .gitignore 忽略）。
# 路径含中文与空格，统一在此登记；脚本一律通过 io/paths.py 读取，禁止硬编码。
data_root: "D:\\path\\to\\光储充\\数据"

acn_project: "${data_root}\\ACN-data\\acn_project"
static_root: "${data_root}\\ACN-data\\ACN-Data-Static\\time series data"
acn_full: "${data_root}\\ACN-data\\acn_full"

output_root: "D:\\path\\to\\light_storage_ev-v2\\patent_preexperiment\\results"
