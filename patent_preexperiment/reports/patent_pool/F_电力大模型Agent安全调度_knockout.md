# F_电力大模型Agent安全调度_knockout

> 候选编号：F
> 方向：电力大模型/Agent 安全调度
> 评审日期（UTC）：2026-09-07
> 性质：cheap knockout（PD 轨，套五问；治理见 `patent_definition/13_治理修订二_…`）。只判"是否值得深挖"，不输出效果级结论。

## 五问

### ① 这是实际工程里真实存在的动作/矛盾吗？
LLM 直接控设备不安全，但运行事件理解 + 工具调用 + 安全校核的需求真实。真实热点。

### ② 能不能说清设备、数据、状态和控制动作？
- 设备：EMS / 可控设备
- 数据：告警 / 状态 / 调度要求 / 工具输出
- 状态：候选策略安全校核状态
- 动作：LLM 编排 → 确定性工具 → 安全校验 → EMS → 审计回写

### ③ 有没有至少一种可实施机制？
运行事件 → LLM 识别任务 → 选确定性工具（潮流 / 优化 / 约束检查）→ 候选方案 → 物理安全校验器 → EMS → 审计链。

### ④ 手头数据/标准/仿真能验证其中哪一部分？
- 58 篇大模型资料 + 标准 + 仿真，不依赖完整现场数据；可纯仿真启动
- 能验：工具调用正确性 / 安全校验有效性（仿真）
- 不能验：真实电网部署

### ⑤ 现有技术已经做到什么，我们还能在哪个连接点继续做？
LLM 电力 2025-2026 从问答 → agent / 工具调用 / 审计（用户引综述）；"用 LLM 调度"单点 crowded；区别压"LLM 被约束在可验证 / 可执行 / 可回退电力控制工作流"。

## knockout 判定

- 判定：**PASS**（非常值得开新 PD 方向）
- 理由：数据门槛最低、热点最强、与 A 不碰撞
- 下一步：定位 LLM agent 电力现有技术 + 设计安全校验器机制
- 与候选 A 的边界：F 是调度编排层，A 是设备能力层，正交
