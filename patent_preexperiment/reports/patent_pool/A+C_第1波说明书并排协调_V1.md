# A+C 第 1 波说明书并排协调 V1

> **生成时间（UTC）**：2026-09-10
> **性质**：PD 轨**跨案协调文档**（A 候选 × C 候选）。第一轮**只解决四件事**：必要技术特征隔离、三层材料划分、独立性红线、组合实施例写法约束。
> **不做**：完整说明书全文、权利要求修改、新 prior-art 检索、新实验、**不把 A 与 C 合并为一件专利**。
> **基线**：A = `patent_definition/12_工程现实性反审_核心与优选实现分离及独立权重构.md` §3–§5（核心 A–D + 独立权 v2 五段 + 从属梯重映射）；C = `patent_pool/C_控制资格状态机_Claim_Draft_v1.2.md`（权 1 五段 + 权 2–9 = D1–D8 + 权 10）；架构定位 = `patent_pool/A-B-C-F_横向组合与专利组合架构评审_V1.md` §5.3/§5.4。
>
> **评审状态（2026-09-10，基于远端 `d69d357` 复核）**：**PASS WITH DRAFTING NOTES** —— 八维隔离、三层材料、"删除对方后仍可实施"强检验、"通信＝排除偏差 vs 资格降级"边界均判定有效；**无架构阻塞**，可直接开始 C 说明书骨架。
>
> **本文件就地修订说明（起草注记 1–4）**：按评审意见修订四处**执行口径**，**不改架构、不涉及任何权利要求语言**，故按项目版本规则**不新建版本**。修订内容见下表，对应章节已同步更新。

### 起草注记落实（评审意见 → 本文件落点）

| # | 评审口径 | 落点 |
|---|---|---|
| **注记 1** | Open item 1 **直接定掉**：同日/近同时窗协调提交；**两案说明书各自 self-contained**；**不以另一申请作为支持来源**（不做 incorporation by reference，说明书层面也不写"参见另一申请"）；可选组合实施例**各自独立写全** | §5.3 新增第 5 条红线；§9 Open item 1 关闭 |
| **注记 2** | §5.2"必须写成本领域常规"**措辞软化**——避免不必要的现有技术自认，为将来另有同步/数据质量机制专利留余量 | §5.2 总纪律改写 |
| **注记 3** | 八维表中"A = 连续 / C = 离散"**仅作典型实施形态**，**不得升级为两案的法律隔离基础**；真正的法律区别 = **被更新的技术对象**（与 F6 / A-M6 的处理方式一致） | §2.1 新增；§2 表"状态空间性质"行加注 |
| **注记 4**（P3） | §7.1"A 案尤不得使用'资源池'"**过硬**——禁止的应是**C 语义的资格准入资源池 / 控制资源子集**，而非"资源池"三个字本身 | §7.1 改写（起 A 骨架时同步套用） |

---

## 0. 阶段登记（正式）

```text
C 文献 deepening          PASS
C OpenCEM validation      PASS（强）
C 独立权架构反审          CLOSED
C Claim Draft v1.2        PASS
lit45 pressure test       PASS WITH CONDITIONS

C Claim 阶段              CLOSED
下一阶段                  A+C 第 1 波说明书并排协调

core-patent = NO-GO        （不变；不受 C 的 PASS 影响）
Round 5     = NOT STARTED  （同上）
B / F 候选                 = 不在本轮范围
```

---

## 1. 两条必要技术特征链（并排钉死）

**A —— 能力修正链（"能做多少"）**

```text
execution evidence           获取功率控制要求 + 对应实际执行结果
        ↓
attribution                  有效性与能力归因判断（排除通信无效 / 暂态响应偏差）
        ↓
directional capability       方向化可用能力状态（成对方向边界，可带可信度）
        ↓
resource takeover            站级功率缺口 → 可承接贡献重算 → 跨资源定向转移
        ↓
capability update            依实际执行结果回写能力状态或其可信度
```

**C —— 资格门控链（"能否参与、能参与什么"）**

```text
operating / abnormal obs     运行状态信息 + 异常指示信息
        ↓
control qualification        三级有序离散资格 + 第三等级双子状态（仅观测 / 完全阻止）
        ↓
allowed action scope         等级 → 允许控制动作范围（完整 / 受限非空 / 空）
        ↓
resource admission           等级 → 是否纳入可控资源集合（完整子集 / 受限子集 / 均不纳入）
        ↓
qualification recovery       满足恢复判据 → 等级提升 + 动作范围与准入条件重开
```

**一句话关系**：C 回答"**这台设备算不算可调度资源、被允许做什么动作**"；A 回答"**已被调度的资源实际做了多少、缺口转给谁、能转多少**"。

---

## 2. 必要技术特征隔离表（逐维对照）

| 维度 | A 必要技术特征 | C 必要技术特征 | 隔离判定 |
|---|---|---|---|
| **主输入** | 功率控制要求 **+ 对应实际执行结果**（指令—执行**成对**） | 运行状态信息 + 异常指示信息（设备**自身状态观测**） | **不重叠**。A 的输入是"要它做多少 vs 实际做了多少"；C 的输入与"是否下达过指令、下达了多少"完全无关 |
| **中间状态对象** | 方向化可用能力状态（成对方向边界，可带可信度） | 控制参与资格（三级有序离散等级 + 双子状态） | **不重叠**。语义 = "能做多少" vs "可否参与"；性质 = 连续 vs 离散有序 |
| **核心判定** | 有效性与能力归因判断：偏差**是否构成**能力受限的**证据** | 依据运行/异常信息确定**资格等级** | **不重叠**。A 判"偏差能不能算能力证据"；C 判"设备处于哪个资格等级" |
| **第一控制后果（数值/动作级）** | 站级功率缺口 → 可承接贡献重算 → 承接资源及其**功率修正量** | **允许控制动作范围**（完整 / 受限非空 / 空） | **不重叠**。A 产出**数值修正量**；C 产出**动作权限集合** |
| **第二控制后果（集合/结构级）** | 跨设备**定向转移**（把缺口从受限资源转给承接资源） | 是否纳入**可控资源集合**（完整子集 / 受限子集 / 均不纳入） | **需显式区分——见 §4.2**。二者都涉及"资源"，但 A 在**已参与调度的资源之间**分配，C 决定**某设备是否算作可调度资源** |
| **闭环回写** | 依功率修正后的实际执行结果更新**能力状态或其可信度** | 满足恢复判据 → **等级提升** + 动作范围与准入条件重开 | **不重叠**。A 回写**能力知识**；C 回写**资格等级** |
| **触发源** | 执行偏差（指令 vs 实际） | 设备运行 / 异常观测变化 | **不重叠** |
| **状态空间性质** | 连续（边界数值 + 可信度） | 离散有序（三级）+ 第三等级双子状态 | **仅作典型实施形态，不作为法律隔离基础——见 §2.1** |

**结论**：八个维度上两案必要技术特征**无一项构成对方的必要前提**。唯一需要额外写清的是"资源"与"通信"两处**事实上的输入域重叠**（§4）。

### 2.1 法律隔离的真正基础 = 被更新的技术对象（**注记 3**）

上表"状态空间性质"一行的 **"A = 连续 / C = 离散"只是两案当前的典型实施形态**，**不得**作为两案的法律隔离基础。理由：

- A 后续完全可能把可信度、能力档位做成**离散**表达（例如能力等级、分档边界）；
- C 也可能内部保存**连续**评分再映射到离散资格（C 权 2 只要求"离散等级"为输出，未限制内部实现）。

一旦把"连续 vs 离散"当作隔离依据，任一方在答审或分案中调整表达形式，隔离基础即被自己动摇。

**真正不可动的区别，是"被更新的技术对象"**——这与横向评审已对 F6 与 A-M6 采用的处理方式一致：

```text
A 的更新对象：设备持续能力知识 / 方向化能力边界
             （"这台设备能输出/吸收多少"的知识，依执行证据更新）

C 的更新对象：设备控制参与资格 / 动作准入权限
             （"这台设备是否被允许参与控制"，依运行与异常观测更新）
```

**写法要求**：两案说明书在陈述区别时，**应落在更新对象上**，而非数据类型上。若确需提及数据形态，只能表述为"在一种实施方式中……"（典型实施例语气），不得表述为"本申请与……的区别在于数据类型不同"。

> **注**：删除对方内容后能否成立，密钥在更新对象不同，不在数据类型不同——§6.2 检查表第 3 项应据此判读。

---

## 3. 交叉污染负向清单（禁止句式）

### 3.1 C 案说明书**不得**出现

- 「根据执行偏差（功率控制要求与实际执行结果之差）修正或降低控制参与资格」
- 「允许控制动作范围由执行偏差推导得出」
- 把 `告警 → 资格 → 资源集合准入` 写成**依赖"当前能力边界/可承接能力"**的判定
- 把 A 的「方向化可用能力状态 / 能力边界 / 承接资源 / 功率修正量 / 等效调节余量」引为必要或从属特征

### 3.2 A 案说明书**不得**出现

- 「根据设备告警或异常指示信息确定资格等级，进而决定是否纳入可控资源集合」作为必要特征
- 把"设备控制参与资格"设为能力状态更新的**前置门**（A 的【b】只做**归因过滤**，不做**资格判定**）
- 把 A 的【d】写成"从**具备控制参与资格**的资源中选择承接资源"
- 把 C 的「控制参与资格 / 允许控制动作范围 / 可控资源集合 / 完整或受限控制资源子集」引为必要或从属特征

---

## 4. 边界易混点（本协调文档识别出的最高风险）

### 4.1 「通信」在两案中的角色不同 —— **最高风险边界**

两案都要用到通信状态，这是**事实上的共同输入域**。但技术意义与后果**完全不同**：

| | A【b】 | C 权 1【a】+ 权 6/D3 |
|---|---|---|
| 作用对象 | **偏差**（指令 vs 实际之差） | **设备状态**（异常指示信息） |
| 逻辑动作 | **排除**（通信无效对应的偏差**不进入**能力证据） | **降级**（通信异常**指示**是资格降级的输入） |
| 目的 | 防止把通信问题**误判为能力不足** | 决定设备**是否仍可被控制** |
| 后果 | **不更新**能力状态 | **等级下降**（可及资源集合收缩） |
| 一句话 | 「这种偏差**不算**能力证据」 | 「这种状态**使设备失去**资格」 |

**写法要求**：
- A 案在【b】处定义"通信无效偏差"时，**不得**写成"通信异常导致资格下降"；
- C 案在【a】处定义"异常指示信息"时，**不得**写成"依据执行偏差判定异常"。

**判定**：两案的通信用法**可索引**（前者做减法、后者做降级），但**必须在各自说明书中给出各自定义位**，否则极易被审查员视为同一技术手段的两种表述，进而引发互吞或双案实质同一的攻击。

### 4.2 「资源」在两案中的角色不同

| | A【d】 | C 权 1【d】/ 权 5 |
|---|---|---|
| 问题 | "在**已被调度**的资源中，谁还能多接一点" | "某设备**是否进入**可调度范围" |
| 前提 | 这些资源**已经在**可调度范围内 | **无前提** |
| 层次 | **分配层** | **准入层** |

- 逻辑顺序上 C 在 A 之前，**但两案独立权都不得要求对方存在**：
  - A 的【d】必须写成"从**多个可控资源**中选择承接资源"（**不写**"从具备资格的资源中"）
  - C 的【d】必须写成"确定是否纳入**参与调度的可控资源集合**"（**不写**"依据其可用能力"）
- 只有到 §6.3 的组合实施例（③ 层）才写"C 先过滤、A 后分配"。

### 4.3 「运行」一词的两种用法

| 案 | 用语 | 含义 |
|---|---|---|
| A【d】 | **运行约束** | 分配时的物理/安全边界（SOC、温度、额定限值、控制模式、爬坡率……），定义位在 A 说明书 |
| C 权 1【a】 | **运行状态信息** | 设备当下处于何种**运行状态**（在运 / 停机 / 检修） |

**写法要求**：两案说明书首次出现处各自加限定语（A 用"运行约束"，C 用"运行状态信息"），**不做同义替换、不互相引用**。

---

## 5. 三层材料划分

### 5.1 ① 两案独占段

| 案 | 独占内容 | 落位 |
|---|---|---|
| **A** | 偏差归因判定（含通信无效/暂态响应排除）、方向化可用能力状态维护、站级功率缺口与可承接贡献重算、跨资源定向转移与功率修正量确定、执行反馈回写 | A 说明书"发明内容"+"具体实施方式"主体；A 独立权与从属梯 |
| **C** | 运行/异常信息 → 三级资格（含第三等级双子状态）、等级 → 允许控制动作范围、等级 → 控制资源子集准入、恢复判据与权限重新开放、渐进恢复顺序 | C 说明书"发明内容"+"具体实施方式"主体；C 独立权与从属梯 |

**纪律**：独占段**不得**出现对方的状态对象名（检查表见 §7）。

### 5.2 ② 可共享基础设施段

| 共享项 | 两案为何都要 | 共享写法约束 |
|---|---|---|
| 设备通信接口与协议（IEC 61850 / Modbus / 厂商私有） | 都要从设备取数据 | 按 §5.2 总纪律口径表述（不承担创造性、不构成必要限定） |
| 遥测采集与时戳对齐 | 两案输入都需时间对齐 | 同上 |
| 数据质量检查（缺测 / 跳变 / 延迟标记） | 两案都用 | 同上。**注意**：共享的是"检查动作"，**不共享"检查结果的技术用途"**——C 中数据质量标记是**资格降级的输入之一**（权 6/D3），A 中则服务于**偏差有效性判定** |
| EMS / 站级控制器 / 边缘控制器 | 两案的执行主体 | 只做硬件/软件载体描述 |
| 可控设备接口（PV / BESS / EV / 充电设备） | 两案的受控对象类型 | 只共享"**设备类型清单**"；**不共享"各类型的映射规则"**（A 的等效调节余量映射、C 的等级映射各自独占） |

**共享段总纪律（注记 2：措辞软化）**：共享段只能出现在说明书的**背景技术**与**具体实施方式**中，并统一采用如下口径表述：

> 共享基础设施可采用本领域已知或常规实现，本申请不以该基础设施本身作为主要创造性贡献；其具体实现不构成以下核心机制的必要限定。

**任何共享段内容都不得进入任一案的独立权。** 共享段的目的是让两案说明书**互相读起来不矛盾**，而不是建立依赖。

**为什么软化（评审理由）**：原稿要求"必须写成本领域常规"，等于为了证明 A/C 独立，把所有通信、时戳对齐、数据质量处理、边缘控制器**一概主动自认现有技术**（admission）。这有两项代价：① 无必要地缩小本案可主张的范围；② 若将来要就某个具体的同步机制或数据质量机制另行申请，会被现在的说明书留下不必要的现有技术痕迹。软化后的口径**既保留"该基础设施不承担创造性、不构成核心机制必要限定"的效果**，又**不做超出必要的自认**。

### 5.3 ③ 可选组合实施例

**写法模板（两案应采用实质一致、各自段落独立完整的表述）**：

> 可选地，在另一实施例中，上述站级功率控制可与设备控制参与资格判定相结合：先根据目标设备的运行状态信息与异常指示信息确定其控制参与资格，将不满足参与条件的设备排除于参与调度的可控资源集合之外；再对参与调度的设备执行上述执行偏差归因与能力描述信息的更新，并据此确定站级功率缺口与承接资源。

**关键：组合实施例中不得直接把对方的核心状态对象写成必要技术（**注记 1**）。**

- ❌ 不要写：「根据**方向化可用能力状态**……」
- ✅ 应写：「可选地，调度器还可结合**表示目标设备可调节能力的能力描述信息**进行功率分配。」

即：本案组合实施例中只出现**上位、中性的能力描述信息**；该"能力描述信息"在**对方自己的说明书**中再具体展开为其核心状态对象。这样两份说明书**能接口、又不互吞**。

**双输入示意**：

```text
                        ┌─ C: 控制参与资格 ────────→ 谁可被调度（准入层）
站级调度输入 ───────────┤
                        └─ A: 能力状态 / 可承接贡献 → 谁能承担多少（分配层）
```

**五条红线**：

1. 必须以「**可选地 / 在另一实施例中 / 进一步地**」引入；
2. **不得写入任一案的独立权**；
3. **不得写成互为必要特征**——反向依赖（如"若资格为第一等级则执行上述归因"）同样**禁止**进入独立权；
4. **组合实施例不得用于独立性论证**（不能因组合有效而声称单案不完整，也不能因单案已完整而否认组合价值）；
5. **两案自足（注记 1）**：组合实施例在本案说明书中**写全**，**不写"参见另一申请"**，**不以另一申请作为支持来源**，**不做 incorporation by reference**。同理，本案组合实施例不得依赖对方申请文本才能实施。

### 5.4 申请策略（注记 1：Open item 1 已定）

```text
同日 / 近同时窗协调提交
+ 两案说明书各自 self-contained
+ 不以另一申请作为支持来源
+ 可选组合实施例各自独立写全
```

**明确不做**：说明书层面"参见另一申请"、incorporation by reference、以及任何使两案独立权互相依赖的引用形式。这样最干净，也免除了答审中"两案互相支持"引发的独立性与重复授权质疑。

---

## 6. 独立性红线与检查表

### 6.1 核心红线

> **A 独立实施例不能要求 C 存在；C 独立实施例不能要求 A 存在。**

### 6.2 检查表（定稿前每案各过一遍）

| # | 检查项 | A 案 | C 案 |
|---|---|---|---|
| 1 | 独立权中不出现对方的状态对象名 | ☐ 无"控制参与资格 / 允许控制动作范围 / 可控资源集合 / 控制资源子集" | ☐ 无"方向化可用能力状态 / 能力边界 / 执行偏差归因 / 承接资源 / 功率修正量" |
| 2 | 从属梯中也未把对方状态对象设为必要特征 | ☐ | ☐ |
| 3 | **删除对方全部内容后，本案说明书仍完整可实施** | ☐ | ☐ |
| 4 | 组合实施例均以"可选地/另一实施例"引入，未进权项 | ☐ | ☐ |
| 5 | 共享基础设施段均表述为"本领域常规" | ☐ | ☐ |
| 6 | 两案对同一物理事实（通信、设备类型清单）的表述**不矛盾** | ☐ | ☐ |
| 7 | 通信相关表述不互吞（§4.1） | ☐ | ☐ |
| 8 | 资源相关表述不互吞（§4.2） | ☐ | ☐ |
| 9 | "运行"用词各自加限定语（§4.3） | ☐ | ☐ |

**第 3 项是最强的单条检验**：若删掉对方内容后本案说明书无法实施，说明独立性已被破坏。

---

## 7. 术语隔离表

### 7.1 独占词（不得跨案借用）

| 概念 | A 用词 | C 用词 |
|---|---|---|
| 状态对象 | 方向化可用能力状态（/ 方向化可信能力边界） | 控制参与资格 |
| 权限表达 | （A 无资格概念） | 允许控制动作范围（完整 / 受限非空 / 空） |
| 集合 | 承接资源 / 候选资源 | 可控资源集合 / 完整控制资源子集 / 受限控制资源子集 |
| 数值量 | 功率修正量 / 等效调节余量 / 站级功率缺口 | （C 无对应概念） |
| 判定 | 归因判断 / 有效性判断 | 资格判定 / 等级更新 / 恢复判据 |
| 闭环 | 执行反馈回写（能力状态或其可信度） | 等级提升 + 权限重新开放 |

**明令禁用**：C 案不得使用 A 的「方向化 / 能力边界 / 等效调节余量 / 功率修正量」；A 案不得使用 C 的「控制参与资格 / 资源集合准入 / 控制资源子集」。

**关于"资源池"（注记 4：口径放宽）**：原稿写"A 案尤不得使用'资源池'"**过硬**——禁止的应是 **C 语义的资格准入资源池 / 控制资源子集**，而非"资源池"三个字本身。若 A 需要描述"承接候选资源的集合"（分配层的候选来源），**允许**使用合适术语（如"承接候选资源""候选资源集合"），只要其**不承担"谁有资格进入"的 C 语义**。

**判定标准是语义而非字面**：

| | A 的集合 | C 的集合 |
|---|---|---|
| 准入依据 | **可承接能力 + 运行约束** | **控制参与资格** |
| 回答的问题 | "谁还能多接一点" | "谁被允许参与控制" |

若两者在同一句中出现，必须**分别标明各自依据**。此项在起 A 骨架时同步套用。

### 7.2 允许共用、但必须各自独立定义的词

| 共用词 | 要求 |
|---|---|
| **可控资源** | 两案都可使用，但**各自说明书必须给出各自的定义位**，且定义中**不得引用对方概念**。（A 语境 = 可接受功率控制要求的资源；C 语境 = 参与调度的可控资源集合的成员资格判定对象） |
| **设备 / 站级 / 电站** | 共用基础词，各自定义位即可 |
| **通信 / 运行** | 共用，但必须按 §4.1 / §4.3 加各自限定语 |
| **能源管理系统（EMS）** | 共用载体词，只作环境描述 |

---

## 8. 第一轮交付物与后续

### 8.1 本轮交付物

- **本文件**：A+C 第 1 波说明书并排协调 V1（四件事：隔离表 / 三层材料 / 独立性红线 / 组合写法）。

### 8.2 后续轮次（不在本轮）

| 轮次 | 内容 | 前置 / 状态 |
|---|---|---|
| 第 2 轮 | A 说明书骨架 | ✅ **已完成（17 号）** —— `patent_definition/17_A说明书骨架_V1.md`（前置 12 §4 评审已由 14 号满足；义务清单按 14/15/16 号落位；背景技术按 10 §4 引用位） |
| 第 2 轮 | C 说明书骨架 | ✅ **已完成** —— `reports/patent_pool/C_控制资格状态机_说明书骨架_V1.md`（PASS WITH DRAFTING GUARDS，已执行完毕） |
| 第 2 轮 | C 说明书全文 | ✅ **已完成** —— `C_控制资格状态机_说明书全文_V1.md`（§1–§15 + 附录 A 内部证据档） |
| 第 3 轮 | A 从属梯 v2 全量语言（权 2–11） | ✅ **已在 14 号 §3 完成**（15 号修订权 2/3/6/7/8） |
| 下一轮 | **A 说明书全文**（按 17 号骨架 §1–§16 展开） | 17 号骨架复核通过 |
| 再下一轮 | **A+C 第二轮并排校验**（两案全文就绪后互相校验术语与污染） | A 全文完成 |
| 定稿期 | C 权 5 措辞替换（见 §9 open item 3） | 交代理/定稿 |

### 8.3 编号与落位约定

- **跨案文档**（如本文件、A-B-C-F 横向评审）→ `reports/patent_pool/`；
- **单案文档**（如权利要求草案、说明书骨架、机制拆解）→ `reports/patent_definition/`（A 案）或 `reports/patent_pool/`（C 案）；
- **A 系列下一可用编号 = 18**（13 号 = 治理修订二；14 号 = A 独立权 v2 收口；15 号 = Claim 语言收尾；**16 号 = A 权利要求语言 v2 最终稿，当前权利要求权威稿**；**17 号 = A 说明书骨架 V1**）。
- **A 侧编号体系已于 14 号冻结、15/16 号沿用**：权 1 方法独立权 / 权 2–11 方法从属权 / **权 12 系统独立权**。原 12 号 §4.2 曾把系统权编为"权 10"，与 §5 从属梯（权 10 = PCC 优选实施、权 11 = 功率控制要求实施形式）冲突，已按「**牺牲编号、保住十级退守位**」处置。C 侧为权 1 / 权 2–9 / 权 10；两案各自独立编号，**不构成相互依赖**。
- **A Claim 阶段已于 16 号 CLOSED**（A Claim v2 = PASS）；当前 A 侧阶段 = 说明书骨架已完成，下一步 = A 说明书全文。

---

## 9. Open items（第一轮不决定）

| # | Open item | 待决 |
|---|---|---|
| 1 | ~~"交叉引用"的具体形式~~ | **已关闭（2026-09-10 评审，注记 1）**：定为——**同日 / 近同时窗协调提交 + 两案说明书各自 self-contained + 不以另一申请作为支持来源 + 可选组合实施例各自独立写全**。**明确排除**说明书层面"参见另一申请"与 incorporation by reference；原稿"说明书层面互相提及"**已被评审取代，不再采用**。落点见 §5.4 |
| 2 | 共享基础设施段是否需在两案说明书中**逐字一致** | **实质已定**：采取"实质一致、措辞各自适配"；总纪律按注记 2 改为软化口径（§5.2）。剩余仅为措辞级适配，随骨架自行处理 |
| 3 | **C 权 5 定稿期措辞替换**（非阻塞、不另开版本）：现为"所述可控资源集合包括完整控制资源子集和受限控制资源子集；……第三等级不纳入二者中任一者"。因"包括"为开放式表达，纯集合逻辑上可能被读成"还存在第三种未命名子集"。定稿/代理阶段可替换为：「**当控制参与资格为第三等级时，不将所述目标设备纳入所述可控资源集合。**」或「所述可控资源集合**至少划分为**完整控制资源子集和受限控制资源子集；……第三等级的目标设备**不作为参与调度的可控资源**」 | 定稿 / 代理 |
| 4 | 共用词"可控资源"在两案说明书中的定义位写法（§7.2） | 说明书骨架 |
| 5 | 若答审中一方需引用对方内容 → 按 09 冻结规则 = **新版本 + 重开检索判定** | 答审 |

---

## 10. 红线自检（本文件自身）

| 检查项 | 结果 |
|---|---|
| 未修改任何权利要求语言 | ✅ 本文件不含权项修改 |
| 未扩 prior-art、未开新检索 | ✅ |
| 未把 A 与 C 合并为一案 | ✅ 通篇按两案处理 |
| 独立权互不依赖已落为可执行检查表（§6.2） | ✅ 9 项，含"删除对方后仍可实施"强检验 |
| 组合实施例未进任何独立权 | ✅ §5.3 **五条**红线（注记 1 增第 5 条"两案自足"） |
| 注记 1 两案自足策略已落地 | ✅ §5.3 红线 5 + §5.4 + §9 item 1 关闭 |
| 注记 2 共享层措辞已软化 | ✅ §5.2（不再一概自认现有技术，仅保留"不承担创造性、不构成必要限定"） |
| 注记 3 法律隔离基础已改为"被更新的技术对象" | ✅ §2.1；连续/离散降为典型实施形态 |
| 注记 4 "资源池"改为语义判定（非字面禁令） | ✅ §7.1 |
| 未产出收益量化结论 | ✅ |
| core-patent / Round 5 状态未被触碰 | ✅ |

---

**报告结束。**
